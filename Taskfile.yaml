version: '3'

vars:
  APP_NAME: committed
  BUILD_DIR: .build
  COV_DIR: .coverage

tasks:
  prepare:
    desc: one-time post-clone repo setup
    cmds:
      - go mod tidy
      - 'echo "NYI: bootstrapping committed as the commit-msg hook?"'

  run:
    desc: run from code
    cmds:
      - go run ./... {{.CLI_ARGS}}

  example:
    desc: run example case (dummy test file)
    aliases: [ ex ]
    cmds:
      - 'printf "chore: dummy message\n\nYou can edit this.\n" > MSG'
      - task: run
        vars: { CLI_ARGS: MSG message }
      - cat MSG
      - rm MSG

  test:
    desc: run all tests, create coverage
    vars:
      # COV_DIR: .coverage
      COV_FILE: '{{.COV_DIR}}/coverage.out'
    cmds:
      - mkdir -p {{.COV_DIR}}
      - go test ./... -coverprofile={{.COV_FILE}}
      - go tool cover -html={{.COV_FILE}} -o {{.COV_DIR}}/coverage.html
      - go tool gcov2lcov -infile {{.COV_FILE}} -outfile {{.COV_DIR}}/lcov.info

  test:watch:
    desc: test in watch mode
    watch: true
    sources: [ '**/*.go' ]
    cmds:
      - task: test

  test:show:
    desc: show test coverage
    deps:
      - test
    cmds:
      - xdg-open {{.COV_DIR}}/coverage.html

  clean:
    desc: clean all generated files
    cmds:
      - rm -rf {{.BUILD_DIR}}
      - rm -rf {{.COV_DIR}}

  build:
    desc: build the binary to '{{.BUILD_DIR}}/{{.APP_NAME}}'
    cmds:
      - mkdir -p '{{.BUILD_DIR}}'
      - go build -o '{{.BUILD_DIR}}/{{.APP_NAME}}' .

  install:
    desc: install from '{{.BUILD_DIR}}/{{.APP_NAME}}'
    vars:
      GOPATH:
        sh: go env GOPATH
    deps:
      - build
    cmds:
      # should this use `go install` instead?
      - cp '{{.BUILD_DIR}}/{{.APP_NAME}}' '{{.GOPATH}}/bin/{{.APP_NAME}}'

  lint:
    desc: run go vet as a lightweight lint step
    cmds:
      - go vet ./...

  tag-release:
    desc: tags the next release if warranted (and on CI)
    vars:
      VER_CUR:
        sh: go tool svu current
      VER_NEXT:
        sh: go tool svu next
    cmds:
      - cmd: echo "svu says {{.VER_CUR}} -> {{.VER_NEXT}}"
        silent: true
      - if: '{{ne .VER_NEXT .VER_CUR}}'
        task: tag-release:ci
        vars:
          VERSION: '{{.VER_NEXT}}'
      - if: '{{eq .VER_NEXT .VER_CUR}}'
        cmd: echo "no new tag created"
        silent: true

  # note that tag-release:ci must be run with permissions to write to the
  # contents of the repo
  tag-release:ci:
    internal: true
    preconditions:
      - sh: '[ "${CI}" == "true" ]'
    requires:
      vars: [ VERSION ]
    cmds:
      - git config user.name "github-actions[bot]"
      - git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
      - git tag -a "{{.VERSION}}" -m "release {{.VERSION}}"
      - git push --tags

  release:test:
    desc: test the release process
    cmds:
      - go tool goreleaser release --clean --snapshot --skip=publish --skip=sign --skip=sbom {{.CLI_ARGS}}

  # note that release must be run with permissions to write to the
  # artifacts and releases of the repo
  release:
    desc: actually release the tool
    preconditions:
      - sh: '[ "${CI}" == "true" ]'
    cmds:
      # skipping signing for now
      - echo "CI is [${CI}]"
      - go tool goreleaser release --clean --skip=sign
